

                        {{ form_start(form) }}
                        <div class="row">
                            <div class="col-sm-6">
                                {{ form_row(form.title) }}
                                {{ form_row(form.description) }}
                                {{ form_row(form.surface) }}
                                {{ form_row(form.rooms) }}
                                {{ form_row(form.bedrooms) }}
                                {{ form_row(form.floor) }}
                            </div>

                            <div class="col-sm-6">
                                {{ form_row(form.price) }}
                                {{ form_row(form.heat) }}
                                {{ form_row(form.city) }}
                                {{ form_row(form.address) }}
                                {{ form_row(form.postal_code) }}
                                {{ form_row(form.sold) }}
                            </div>
                            <div>{{ form_row(form.created_at) }}</div>

                        </div>
                        <button type="submit" class="btn btn-outline-success">{{ button|default('Enregistrer') }}</button>
                        {{ form_end(form) }}