{% extends 'base.html.twig' %}

{% block title %}MicImmo - Edition -{{ option.name }} - {% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row table-container">

            <div class="col-sm-3">
                {% include 'includes/leftmenu.html.twig' %}
            </div>
            <div class="col-sm-9">
                <ul class="list-group">
                    <h1 href="#" class="h1-titre list-group-item list-group-item-primary active">
                        <i class="fas fa-home"></i> Edition de l'option:
                    </h1>

                    <li class="list-group-item">
                        {{ include ('admin_property/option/_form.html.twig', {form: form, button: 'Editer'}) }}
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}

