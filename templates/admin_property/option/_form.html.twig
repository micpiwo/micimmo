

                        {{ form_start(form) }}
                        <div class="row">
                            <div class="col-sm-6">
                                {{ form_rest(form) }}
                            </div>
                        </div>
                        <button type="submit" class="btn btn-outline-success">{{ button|default('Enregistrer') }}</button>
                        {{ form_end(form) }}